#ifndef SLIDESHOW_HPP_INCLUDED
#define SLIDESHOW_HPP_INCLUDED
#include "qualif_lyon.hpp"
#include <bits/stdc++.h>

std::vector<Slide> initial_random(std::vector<Photo>& photos);
std::vector<Slide> greedy_nul(std::vector<Photo>& photos);

#endif // SLIDESHOW_HPP_INCLUDED
