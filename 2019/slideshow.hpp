#ifndef SLIDESHOW_HPP_INCLUDED
#define SLIDESHOW_HPP_INCLUDED
#include "qualif_lyon.hpp"
#include <bits/stdc++.h>
using namespace std;

vector<Slide> initial_random(vector<Photo>& photos);
vector<Slide> greedy_nul(vector<Photo>& photos);

#endif // SLIDESHOW_HPP_INCLUDED
